# Deprecated: MyFusion now uses separate root-level config files.
# Use ../test.yml for UI/CLI inference and ../train.yml for Assess-TPGD training.
# This file is kept only as a pointer for older notes/scripts.
