# Scripts

`run.bash` and `run_cli.bash` remain the primary local entrypoints.

This directory is for small reusable utilities that should also work after the
project becomes a package.

Useful commands:

```bash
python scripts/inspect_assessment_hidden.py outputs/feature_smoke/features/lowlight1_round1_assessment_reasoning_hidden.pt
```
